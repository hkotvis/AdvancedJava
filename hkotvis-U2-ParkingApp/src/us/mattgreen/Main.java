package us.mattgreen;

import java.io.File;
import java.io.FileNotFoundException;
import java.util.ArrayList;
import java.util.Random;
import java.util.Scanner;

import static java.awt.SystemColor.menu;

public class Main { // No scanners or prints... just calling methods

    public static void main(String[] args)  {
        Garage g = new Garage();
        g.welcome();






           //g.printReceipt();
            // $5 for 3 hours, for each additional hour it's + $1
            //Can't charge more than $15 unless lost a ticket ($25)
        }






//The application needs to store all of the tickets in a file.  When the application is opened, that file needs to load all of the tickets in a collection.


    }

